# Union of all services' dependencies, pinned. Used for local development
# and as the dev-requirements base; each Docker image installs only its own
# requirements-<service>.txt from this directory.
discord.py==2.4.0
Flask==3.0.3
python-dotenv==1.0.1
SQLAlchemy==2.0.31
psycopg2-binary==2.9.9
stripe==10.4.0
pika==1.3.2
cryptography==43.0.0
gunicorn==22.0.0
APScheduler==3.10.4
alembic==1.18.5
python-dateutil==2.9.0.post0
